import { DEFAULT_QUOTA, SHIFT_DEFS } from "./constants.js";
import { dayIndex, daysInMonth, isWeekendDay } from "./dates.js";

const dayAllowed = (rule, index) => {
  if (rule === "weekdays") return index >= 0 && index <= 4;
  if (rule === "weekend") return index === 5 || index === 6;
  if (rule === "saturday") return index === 5;
  if (rule === "sunday") return index === 6;
  return true;
};

export function normalizeAuxiliary(aux) {
  return { active: true, status: "available", quota: DEFAULT_QUOTA, lead: false, coverage: false, night: false, days: "all", customDays: [0, 1, 2, 3, 4, 5, 6], shift: "all", ...aux };
}
export function canWorkDay(aux, year, month, day) { if (!aux) return false; const a = normalizeAuxiliary(aux); if (!a.active || a.status === "absent") return false; const index = dayIndex(year, month, day); return a.days === "custom" ? (a.customDays || []).includes(index) : dayAllowed(a.days, index); }
export function canWorkShift(aux, shift, year, month, day) { if (!aux) return false; const a = normalizeAuxiliary(aux); if (!canWorkDay(a, year, month, day)) return false; if (shift === "night") return a.shift === "all" || a.shift === "night" || !!a.night; if (a.shift === "night") return false; if (a.shift === "morning") return shift === "morning"; if (a.shift === "afternoon") return shift === "afternoon"; return true; }
const isWeekendOnly = aux => aux.days === "weekend" || aux.days === "saturday" || aux.days === "sunday";
const prioritizeRomain = team => team.slice().sort((a, b) => Number(/^romain\b/i.test(b.name || "")) - Number(/^romain\b/i.test(a.name || "")));
function pickSequential({ ordered, pointers, key, shift, year, month, day }) { const available = ordered.filter(aux => canWorkShift(aux, shift, year, month, day)); if (!available.length) return null; const offset = (pointers[key] || 0) % available.length, picked = available[offset]; pointers[key] = (offset + 1) % available.length; return picked.id; }
function pickWeekdayOwner({ team, pointers, shift, year, month, day, previous = null }) { const base = team.filter(aux => !aux.coverage && aux.shift !== "night" && !isWeekendOnly(aux)), leader = base.find(aux => aux.lead), others = prioritizeRomain(base.filter(aux => aux.id !== leader?.id)), ordered = leader ? [...others, leader] : others, available = ordered.filter(aux => canWorkShift(aux, shift, year, month, day)).filter(aux => !aux.lead || others.some(teammate => canWorkShift(teammate, shift, year, month, day))); if (!available.length) return null; const offset = (pointers["weekday-owner"] || 0) % available.length, rotated = available.slice(offset).concat(available.slice(0, offset)), picked = rotated.find(aux => aux.id !== previous) || rotated[0], index = available.findIndex(aux => aux.id === picked.id); pointers["weekday-owner"] = (index + 1) % available.length; return picked.id; }
function pickWeekendOwner({ team, pointers, shift, year, month, day }) { return pickSequential({ ordered: team.filter(aux => !aux.coverage && !aux.lead && aux.shift !== "night"), pointers, key: "weekend-owner", shift, year, month, day }); }
function weekendOwnerTeam({ team, previousWorked, shift, year, month, day }) { const canCover = aux => !aux.coverage && aux.shift !== "night" && canWorkShift(aux, shift, year, month, day), main = team.filter(canCover), teammates = main.filter(aux => !aux.lead), restedTeammates = teammates.filter(aux => !previousWorked.has(aux.id)), rested = main.filter(aux => !previousWorked.has(aux.id)); return restedTeammates.length ? restedTeammates : teammates.length ? teammates : rested.length ? rested : main; }
function orderedTeam(team, pointer, preferLeader, preferWeekendOnly) { const base = team.filter(aux => aux.active && aux.status !== "absent"), leader = preferLeader ? base.find(aux => aux.lead) : null; let ordered = base; if (preferWeekendOnly) ordered = [...base.filter(isWeekendOnly), ...base.filter(aux => !isWeekendOnly(aux))]; else if (leader) { const others = base.filter(aux => aux.id !== leader.id); ordered = others.length ? [...others, leader] : [leader]; } if (!ordered.length) return []; const offset = pointer % ordered.length; return ordered.slice(offset).concat(ordered.slice(0, offset)); }
function pickWorker({ team, pointers, shift, year, month, day, previous, exclude = [], preferLeader = false, preferWeekendOnly = false, includeCoverage = false }) { const key = preferWeekendOnly ? `weekend-${shift}` : shift, ordered = orderedTeam(team, pointers[key] || 0, preferLeader, preferWeekendOnly).filter(aux => includeCoverage || !aux.coverage).filter(aux => canWorkShift(aux, shift, year, month, day)); if (!ordered.length) return null; const blocked = new Set([previous, ...exclude].filter(Boolean)), candidates = ordered.filter(aux => !blocked.has(aux.id)), picked = candidates[0] || ordered[0], idx = ordered.findIndex(aux => aux.id === picked.id); pointers[key] = (idx >= 0 ? (pointers[key] || 0) + idx + 1 : (pointers[key] || 0) + 1) % ordered.length; return picked.id; }
function shiftWorkers(entry) { return !entry ? [] : Array.isArray(entry.workers) ? entry.workers.filter(Boolean) : entry.worker ? [entry.worker] : []; }
function addShift(dayPlan, shift, worker, load, extras = [], primaryLoad = load) { const def = SHIFT_DEFS.find(item => item.id === shift), workers = [worker, ...extras].filter(Boolean); dayPlan[shift] = { worker, workers, hours: def?.hours || 0 }; workers.forEach(id => { load[id] = (load[id] || 0) + (def?.hours || 0); }); if (worker && primaryLoad !== load) primaryLoad[worker] = (primaryLoad[worker] || 0) + (def?.hours || 0); }
function pickPreferredOrNext({ preferred, team, pointers, shift, year, month, day, previous, preferLeader = false, preferWeekendOnly = false }) { const preferredAux = team.find(aux => aux.id === preferred); return preferredAux && preferredAux.id !== previous && canWorkShift(preferredAux, shift, year, month, day) ? preferredAux.id : pickWorker({ team, pointers, shift, year, month, day, previous, preferLeader, preferWeekendOnly }); }
function pickLeaderDouble({ primary, team, pointers, shift, year, month, day, weekend, dayDoubles }) { const primaryAux = team.find(aux => aux.id === primary); if (!primaryAux?.lead || weekend) return []; const saved = dayDoubles?.[primary], savedAux = team.find(aux => aux.id === saved?.worker); if (saved?.day >= day - 1 && savedAux && !savedAux.coverage && savedAux.shift !== "night" && canWorkShift(savedAux, shift, year, month, day)) { saved.day = day; return [savedAux.id]; } const teammates = prioritizeRomain(team.filter(aux => !aux.lead && !aux.coverage && aux.shift !== "night")), rotating = saved?.worker ? teammates.filter(aux => aux.id !== saved.worker) : teammates, teammate = pickSequential({ ordered: rotating.length ? rotating : teammates, pointers, key: "leader-double", shift, year, month, day }); if (teammate && dayDoubles) dayDoubles[primary] = { worker: teammate, day }; return teammate ? [teammate] : []; }
function pickCoverageDouble({ primary, team, pointers, shift, year, month, day, dayDoubles }) { const savedId = dayDoubles?.coverage?.[day], saved = team.find(aux => aux.id === savedId); if (saved && saved.id !== primary && saved.coverage && canWorkShift(saved, shift, year, month, day)) return [saved.id]; const candidates = team.filter(aux => aux.coverage && aux.id !== primary), teammate = pickWorker({ team: candidates, pointers, shift, year, month, day, previous: primary, exclude: [primary], includeCoverage: true }); if (teammate && dayDoubles) { dayDoubles.coverage ||= {}; dayDoubles.coverage[day] = teammate; } return teammate ? [teammate] : []; }
function dayExtras({ primary, team, pointers, shift, year, month, day, weekend, dayDoubles }) { if (!primary) return []; const coverage = pickCoverageDouble({ primary, team, pointers, shift, year, month, day, dayDoubles }); return coverage.length ? coverage : pickLeaderDouble({ primary, team, pointers, shift, year, month, day, weekend, dayDoubles }); }
function pickNightOnlyDouble({ primary, team, pointers, year, month, day, weekend }) { const primaryAux = team.find(aux => aux.id === primary); if (primaryAux?.shift === "night") return []; const key = weekend ? "weekend-night-double" : "night-double", candidates = team.filter(aux => aux.shift === "night" && canWorkShift(aux, "night", year, month, day)), teammate = pickWorker({ team: candidates, pointers: { ...pointers, night: pointers[key] || 0 }, shift: "night", year, month, day, previous: primary, exclude: [primary], includeCoverage: true }); if (!teammate) return []; const idx = candidates.findIndex(aux => aux.id === teammate); pointers[key] = (idx >= 0 ? idx + 1 : (pointers[key] || 0) + 1) % Math.max(1, candidates.length); return [teammate]; }
function nightExtras({ primary, team, pointers, year, month, day, weekend, dayDoubles }) { const nightOnly = pickNightOnlyDouble({ primary, team, pointers, year, month, day, weekend }); if (nightOnly.length) return nightOnly; const savedCoverage = team.find(aux => aux.id === dayDoubles?.coverage?.[day]); if (savedCoverage && savedCoverage.id !== primary && canWorkShift(savedCoverage, "night", year, month, day)) return [savedCoverage.id]; return dayExtras({ primary, team, pointers, shift: "night", year, month, day, weekend, dayDoubles }); }
function makeContext(team, initialWeekendRest) { return { schedule: {}, blocks: [], load: Object.fromEntries(team.map(aux => [aux.id, 0])), primaryLoad: Object.fromEntries(team.map(aux => [aux.id, 0])), pointers: { morning: 0, afternoon: 0, night: 0, "weekday-owner": 0, "weekend-owner": 0, "night-double": 0, "weekend-morning": 0, "weekend-afternoon": 0, "weekend-night": 0, "weekend-night-double": 0 }, weekendWorker: null, weekendHandover: initialWeekendRest[0] || null, doubles: {}, weekendKey: "", previousWorked: new Set(initialWeekendRest) }; }
function record(c, plan, year, month) { c.schedule[plan.day] = plan; const worked = new Set(); SHIFT_DEFS.forEach(shift => { shiftWorkers(plan[shift.id]).forEach(worker => c.blocks.push({ day: plan.day, shift: shift.id, worker, hours: shift.hours })); if (plan[shift.id]?.worker) worked.add(plan[shift.id].worker); }); c.previousWorked = worked; if (plan.weekend && dayIndex(year, month, plan.day) === 6) c.weekendHandover = plan.afternoon?.worker || plan.morning?.worker || c.weekendHandover; }
function buildDailySchedule({ year, month, auxiliaries, initialWeekendRest = [] }) { const team = auxiliaries.map(normalizeAuxiliary).filter(aux => aux.active), c = makeContext(team, initialWeekendRest); let previous = null; for (let day = 1; day <= daysInMonth(year, month); day += 1) { const weekend = isWeekendDay(year, month, day), index = dayIndex(year, month, day), available = team, morningTeam = team, monday = index === 0 ? c.weekendHandover : null, key = `${year}-${month}-${index === 5 ? day : day - 1}`; if (!weekend || key !== c.weekendKey) { c.weekendKey = key; c.weekendWorker = null; } const plan = { day, weekend }; let am; if (weekend && c.weekendWorker && canWorkShift(morningTeam.find(aux => aux.id === c.weekendWorker), "morning", year, month, day)) am = c.weekendWorker; else { const eligible = weekend && index === 5 ? weekendOwnerTeam({ team: morningTeam, previousWorked: c.previousWorked, shift: "morning", year, month, day }) : morningTeam; am = day === 1 ? null : monday || (weekend ? pickWeekendOwner({ team: eligible, pointers: c.pointers, shift: "morning", year, month, day }) : pickPreferredOrNext({ preferred: previous, team: morningTeam, pointers: c.pointers, shift: "morning", year, month, day, previous: null, preferLeader: true })) || pickWorker({ team: morningTeam, pointers: c.pointers, shift: "morning", year, month, day, previous, preferLeader: !weekend, preferWeekendOnly: weekend }); if (weekend) { c.weekendWorker = am; c.weekendHandover = am; } } addShift(plan, "morning", am, c.load, dayExtras({ primary: am, team: morningTeam, pointers: c.pointers, shift: "morning", year, month, day, weekend, dayDoubles: c.doubles }), c.primaryLoad); const pm = weekend ? pickPreferredOrNext({ preferred: c.weekendWorker || am, team: available, pointers: c.pointers, shift: "afternoon", year, month, day, previous: null, preferWeekendOnly: true }) : pickWeekdayOwner({ team: available, pointers: c.pointers, shift: "afternoon", year, month, day, previous }) || pickWorker({ team: available, pointers: c.pointers, shift: "afternoon", year, month, day, previous: am, preferLeader: true }); addShift(plan, "afternoon", pm, c.load, dayExtras({ primary: pm, team: available, pointers: c.pointers, shift: "afternoon", year, month, day, weekend, dayDoubles: c.doubles }), c.primaryLoad); const pmAux = available.find(aux => aux.id === pm), night = pmAux && canWorkShift(pmAux, "night", year, month, day) ? pm : pickWorker({ team: available, pointers: c.pointers, shift: "night", year, month, day, previous: am, preferWeekendOnly: weekend }); addShift(plan, "night", night, c.load, nightExtras({ primary: night, team: available, pointers: c.pointers, year, month, day, weekend, dayDoubles: c.doubles }), c.primaryLoad); previous = pm || am || previous; record(c, plan, year, month); } return { schedule: c.schedule, blocks: c.blocks, load: c.load }; }
function buildBlockSchedule({ year, month, auxiliaries, rotationDays, initialWeekendRest = [] }) { const team = auxiliaries.map(normalizeAuxiliary).filter(aux => aux.active), c = makeContext(team, initialWeekendRest), total = daysInMonth(year, month), span = Math.min(4, Math.max(2, Number(rotationDays) || 2)), pattern = span === 3 ? [3, 2, 2] : [span - 1], starts = [], owners = {}; let previous = null; for (let start = 1, pi = 0; start <= total; pi += 1) { const serviceDays = pattern[pi % pattern.length], ownerDay = Array.from({ length: serviceDays }, (_, offset) => start + offset).find(day => day <= total && !isWeekendDay(year, month, day)), owner = ownerDay ? pickWeekdayOwner({ team, pointers: c.pointers, shift: "afternoon", year, month, day: ownerDay, previous }) || pickWorker({ team, pointers: c.pointers, shift: "afternoon", year, month, day: ownerDay, previous, preferLeader: true }) : previous; starts.push({ start, serviceDays }); owners[start] = owner; previous = owner; start += serviceDays; } for (let day = 1; day <= total; day += 1) { const weekend = isWeekendDay(year, month, day), index = dayIndex(year, month, day), available = team, morningTeam = team, monday = index === 0 ? c.weekendHandover : null, plan = { day, weekend }, si = starts.findIndex(block => block.start <= day && day < block.start + block.serviceDays), block = starts[si], before = starts[si - 1]; let owner = owners[block?.start], morningOwner = day === block?.start && before ? owners[before.start] : owner; if (weekend) { const key = `${year}-${month}-${index === 5 ? day : day - 1}`, weekendAux = available.find(aux => aux.id === c.weekendWorker); if (!c.weekendWorker || key !== c.weekendKey || !canWorkShift(weekendAux, "morning", year, month, day)) { c.weekendKey = key; const weekendTeam = index === 5 ? weekendOwnerTeam({ team: available, previousWorked: c.previousWorked, shift: "morning", year, month, day }) : available; c.weekendWorker = pickWeekendOwner({ team: weekendTeam.length ? weekendTeam : available, pointers: c.pointers, shift: "morning", year, month, day }); c.weekendHandover = c.weekendWorker; } morningOwner = c.weekendWorker || morningOwner; owner = c.weekendWorker || owner; } else { c.weekendWorker = null; c.weekendKey = ""; } morningOwner = monday || morningOwner; const am = day === 1 ? null : monday || pickPreferredOrNext({ preferred: morningOwner, team: morningTeam, pointers: c.pointers, shift: "morning", year, month, day, previous: null, preferLeader: !weekend, preferWeekendOnly: weekend }); addShift(plan, "morning", am, c.load, dayExtras({ primary: am, team: morningTeam, pointers: c.pointers, shift: "morning", year, month, day, weekend, dayDoubles: c.doubles }), c.primaryLoad); const pm = pickPreferredOrNext({ preferred: owner, team: available, pointers: c.pointers, shift: "afternoon", year, month, day, previous: am === owner ? null : am, preferLeader: !weekend, preferWeekendOnly: weekend }); addShift(plan, "afternoon", pm, c.load, dayExtras({ primary: pm, team: available, pointers: c.pointers, shift: "afternoon", year, month, day, weekend, dayDoubles: c.doubles }), c.primaryLoad); const pmAux = available.find(aux => aux.id === pm), night = pmAux && canWorkShift(pmAux, "night", year, month, day) ? pm : pickPreferredOrNext({ preferred: owner, team: available, pointers: c.pointers, shift: "night", year, month, day, previous: am, preferWeekendOnly: weekend }); addShift(plan, "night", night, c.load, nightExtras({ primary: night, team: available, pointers: c.pointers, year, month, day, weekend, dayDoubles: c.doubles }), c.primaryLoad); record(c, plan, year, month); } return { schedule: c.schedule, blocks: c.blocks, load: c.load }; }
export function buildSchedule({ year, month, auxiliaries, rotationDays = 1 }) { const days = Number(rotationDays) || 1; return days >= 2 ? buildBlockSchedule({ year, month, auxiliaries, rotationDays: days }) : buildDailySchedule({ year, month, auxiliaries }); }
export function calculateHours(schedule, auxiliaries) { const hours = Object.fromEntries(auxiliaries.map(aux => [aux.id, { morning: 0, afternoon: 0, night: 0, total: 0, quota: Number(aux.quota) || DEFAULT_QUOTA }])); Object.values(schedule).forEach(day => SHIFT_DEFS.forEach(shift => shiftWorkers(day[shift.id]).forEach(worker => { if (worker && hours[worker]) { hours[worker][shift.id] += shift.hours; hours[worker].total += shift.hours; } }))); return hours; }
